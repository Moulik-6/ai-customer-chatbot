# Python packages for Flask chatbot
# Only free dependencies

flask>=2.3,<4
flask-cors>=4.0,<7
flask-limiter>=3.5,<5
requests>=2.31,<3
python-dotenv>=1.0,<2
supabase>=2.3,<3

# NLP — spaCy for semantic intent matching
spacy>=3.7,<4

# ML / training (required for local fine-tuning + inference scripts)
transformers>=4.40,<5
torch>=2.1
accelerate>=0.28,<2
huggingface-hub>=0.22,<1
pyarrow>=15,<22
sentencepiece>=0.1.99,<1
numpy<2